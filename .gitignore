# Added by Klaussy — Claude per-repo permission overrides
.claude/settings.local.json

# Local planning scratch (klaussy plan skill)
plan.md

# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/.next-e2e/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# bundled worker (npm run build:worker)
/dist

# testing
/coverage
/test-results
/playwright-report

# database — the dev db, plus the WAL and shared-memory sidecars
/data/*.db
/data/*.db-*

# env — .env.example is the one that ships
.env*
!.env.example

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.vercel
!/data/.gitkeep

# PR description written by the klaussy pr skill
pr-description.md

# Cached model responses, so a re-run does not cost another call
data/ai-cache/

# npm run eval:dump output: real intake content, so it stays local
/evals/

# Review artifact written by the klaussy review skill
REVIEW_OUTPUT.md
